# Next-Step Backlog

## Near Term

- Add a small browser-based review UI over the CSV data.
- Add a command to promote rows from `pending_review` to `approved` with reviewer initials.
- Add platform-specific copy length checks.
- Add PR creation/update against GitHub once CLI auth is available in automation runs.
- Add brand-aware weekly seed templates instead of the current default Business Signal Workshop pack.

## Data And Reporting

- Store weekly metric snapshots separately from current metrics.
- Add campaign-level recommendations for the next weekly prompt.
- Track approved image asset versions.

## Integrations

- Add optional Google Sheets import/export.
- Add optional scheduler payload export for approved-only posts.
- Add analytics import adapters by platform.
- Add environment-variable based API clients for future scheduling tools.

## Governance

- Add content claim review fields.
- Add reviewer and approved-at metadata.
- Add audit trail for status changes.
